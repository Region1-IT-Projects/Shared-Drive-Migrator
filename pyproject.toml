[tool.poetry]
name = "shared-drive-migrator"
version = "0.1.0"
description = ""
authors = ["cannotilever <jake@cannotilever.xyz>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"
flask = "^3.1.0"
google-api-python-client = "^2.156.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
